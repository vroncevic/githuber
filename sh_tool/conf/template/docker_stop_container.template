${H}"!/bin/bash"
${H}
${H}" @brief   ${BRIEF}"
${H}" @version ${VERSION}"
${H}" @date    ${DATE}"
${H}" @company ${COMPANY}"
${H}" @author  ${AUTHOR_NAME} <${AUTHOR_EMAIL}>"
${H}

"CONTAINER_NAME=${PRO_NAME}"

"docker container stop \\\$CONTAINER_NAME"

"# Success exit"
"exit 0"
